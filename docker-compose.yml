version: '3.7'

services:
  auths:
    build:
      context: .
      dockerfile: Dockerfile
    ports: 
      - 3000:3000
    volumes: 
      - ./src:/app/src
    networks:
      - tada-network
    command: ["npm", "run", "start:dev"]

    
networks:
  tada-network:
    external: true